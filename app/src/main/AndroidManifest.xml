<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Necessário para o Afinador capturar áudio do microfone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Indica suporte preferencial a hardware de áudio de baixa latência.
        required="false" garante que o app instale em dispositivos sem esse hardware,
        mas o sistema preferirá o modo de baixa latência quando disponível.
        Ver: https://developer.android.com/ndk/guides/audio/audio-latency
    -->
    <uses-feature
        android:name="android.hardware.audio.low_latency"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.audio.pro"
        android:required="false" />

    <application
        android:name=".PitchAndMetronomeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PitchAndMetronome">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
